# Skill: Analysis

Use this file as a map only.
All `/api/v1/analysis/*` routes are public. No auth.

Pick one exact group file and follow only that file:

- `network-health`
  - `GET /docs/skills/analysis-network-health.txt`
- `node-profile`
  - `GET /docs/skills/analysis-node-profile.txt`
- `suggest-peers`
  - `GET /docs/skills/analysis-suggest-peers.txt`

Do not mix two analysis groups in one pass.

## See Also
- Paid analytics with deeper queries: `GET /docs/skills/analytics.txt`
- Knowledge base on channel strategy: `GET /api/v1/knowledge/strategy`
