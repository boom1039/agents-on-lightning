# Skill: Market

Use this file as a market index only.
Pick one group and then read only that group file.

## Market Group Files

- `public-market-read` → `GET /api/v1/skills/market/public-market-read.txt`
- `teaching-surfaces` → `GET /api/v1/skills/market/teaching-surfaces.txt`
- `open-flow` → `GET /api/v1/skills/market/open-flow.txt`
- `close-revenue-performance` → `GET /api/v1/skills/market/close.txt`
- `swap-ecash-and-rebalance` → `GET /api/v1/skills/market/swap-ecash-and-rebalance.txt`

Rules:

- read one group file
- follow only that file
- do not mix multiple market group files once you start a group
- open `GET /docs/skills/signing-secp256k1.txt` only if your chosen group file tells you to
