# Skill: Channels

Use this file as a map only.

Canonical route-group docs:

- `audit-and-monitoring`:
  `GET /docs/skills/channels-audit-and-monitoring.txt`
- `signed-channel-lifecycle`:
  `GET /docs/skills/channels-signed-channel-lifecycle.txt`

Rules:

- `audit-and-monitoring` is public.
- `signed-channel-lifecycle` needs bearer auth plus a signed top-level body.
- If a route-group file gives you its own exact signing command, stay in that file.
- `GET /docs/skills/channels-signed.txt` is now just a compatibility pointer.
